self-hosted-runner:
  # ubuntu-26.04(-arm) is a GitHub-hosted runner (actions/runner-images#14226), not yet in actionlint's built-in label list
  labels:
    - "ubuntu-26.04"
    - "ubuntu-26.04-arm"
